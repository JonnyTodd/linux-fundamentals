# Step 6 — Independent mini-challenge

For this final step, the exact commands are **not** provided.

Create the following directory structure inside `~/linux-lab1`:

```text
submission/
└── week1/
```

Inside the `week1` directory, place copies of:

```text
linux-notes.txt
welcome-copy.txt
clue.txt
```

Then return to the original `practice` directory and **delete**:

```text
commands.txt
```

You may find these commands useful:

```text
pwd
ls
cd
mkdir
cp
rm
```

## Final required result

Your submission directory should contain:

```text
~/linux-lab1/submission/week1/linux-notes.txt
~/linux-lab1/submission/week1/welcome-copy.txt
~/linux-lab1/submission/week1/clue.txt
```

and this file should **no longer exist**:

```text
~/linux-lab1/training/practice/commands.txt
```

Select **CHECK** when you believe the challenge is complete.
