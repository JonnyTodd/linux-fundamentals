# Lab 1 complete

You have now used a real Linux command line to:

- identify your system;
- navigate directories;
- create files and directories;
- copy and rename files;
- reveal hidden files;
- delete a file;
- complete a small task without step-by-step commands.

## Commands introduced

```text
whoami
hostname
uname
pwd
ls
ls -la
cd
mkdir
touch
cat
cp
mv
rm
```

Before the next lab, make sure you can answer:

1. What does `pwd` tell you?
2. What does `..` mean?
3. What does `~` mean?
4. How do you show hidden files?
5. Which command renames a file?
6. Why should you be cautious with `rm`?

The environment is disposable, so it is fine to close it when your tutor asks you to.
