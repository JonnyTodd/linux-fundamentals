# Step 1 — Re-orient yourself

Start with the troubleshooting routine from Lab 1:

```bash
pwd
ls
```

The prepared organisation files are stored in:

```text
~/linux-lab2/company
```

Move there and inspect the contents:

```bash
cd ~/linux-lab2/company
pwd
ls
cat README.txt
```

Now look inside the two prepared directories:

```bash
ls logs
ls records
```

There is no CHECK on this step. Continue when you can identify the three log files, the staff file and the empty `reports` directory.

